const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

// instead of writing the function all the way out,
// you can use const (var name) = (parameter) => {

// }
